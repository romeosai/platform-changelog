# 平台組大事記 · Platform Changelog

金猴爺平台組的時間軸式上線紀錄頁面，純前端（HTML/CSS/JS，無框架），記錄每一次上線的時間、目的與預期成效。

- **線上網址**：<!-- GitHub Pages 部署後補上 -->
- **維護**：車海仁
- **內容來源**：`index.html` 為實際頁面（含互動篩選、時間軸UI），`platform-changelog.md` 為同步的純文字/Markdown版本，方便快速閱讀或分享。

## 本機開發

純靜態頁面，直接用瀏覽器開啟 `index.html` 即可，不需要任何建置步驟。

## 部署

透過 GitHub Pages 部署（Settings → Pages → Source: Deploy from branch → main → / (root)）。
